import styled from 'styled-components';

const Styled{{pascalCase pageName}} = styled.div`
  color: #000000;
`;

export const {{pascalCase pageName}} = () => (
  <Styled{{pascalCase pageName}} data-testid="{{pascalCase pageName}}">
    {{pascalCase pageName}}
  </Styled{{pascalCase pageName}}>
);
