import styled from 'styled-components';

const Styled{{pascalCase componentName}} = styled.div`
  color: #000000;
`;

export const {{pascalCase componentName}} = () => (
  <Styled{{pascalCase componentName}} data-testid="{{pascalCase componentName}}">
    {{pascalCase componentName}}
  </Styled{{pascalCase componentName}}>
);
