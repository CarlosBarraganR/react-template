import { render } from '@testing-library/react';
import { {{pascalCase componentName}} } from '../{{pascalCase componentName}}';

describe('{{pascalCase componentName}}', () => {
  test('renders', () => {
    const { getByTestId } = render(<{{pascalCase componentName}} />);
    expect(getByTestId('{{pascalCase componentName}}')).toBeInTheDocument();
  });
});

